// components/ui/AlertTitle.js
import React from 'react';

const AlertTitle = ({ children }) => {
  return <strong className="font-bold">{children}</strong>;
};

// Exporting AlertTitle as default
export default AlertTitle;
